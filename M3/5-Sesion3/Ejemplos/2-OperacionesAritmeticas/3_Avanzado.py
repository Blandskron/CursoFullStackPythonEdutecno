# Usando la función pow() (pow Calcula la potencia d un numero) y operaciones mixtas
import math

a = 5
b = 2
resultado = math.pow(a, b) + (a * b) - (a / b)
print("Resultado de la operación mixta:", resultado)
