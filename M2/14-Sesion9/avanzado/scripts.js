// Cambia el color de todos los elementos de una lista de clases
var elementos = document.getElementsByClassName("miClase");
for(var i = 0; i < elementos.length; i++) {
    elementos[i].style.color = "blue";
}
